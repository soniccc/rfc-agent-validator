[project]
name = "rfc-agent-validator"
version = "0.1.0"
description = "RFC analysis agent with empirical validation comparing custom API tools vs WebSearch-only implementation"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.1.18",
    "httpx>=0.27.0",
]
